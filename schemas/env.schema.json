{
  "schema_version": "0.1",
  "title": "UMC public env.json (minimal)",
  "type": "object",
  "required": ["schema_version", "generated_at_utc", "python", "platform"],
  "properties": {
    "schema_version": { "type": "string" },
    "generated_at_utc": { "type": "string" },
    "python": { "type": "string" },
    "platform": {
      "type": "object",
      "required": ["system", "release", "version", "machine", "processor"],
      "properties": {
        "system": { "type": "string" },
        "release": { "type": "string" },
        "version": { "type": "string" },
        "machine": { "type": "string" },
        "processor": { "type": "string" }
      }
    },
    "notes": { "type": "string" }
  }
}

